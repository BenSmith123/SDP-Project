// hack to change back to attack key when leaving - without affecting other npc chats not changning the sprite
if instance_exists(obj_arrow_key_attack) { obj_arrow_key_attack.sprite_index = spr_mobile_key_attack }
